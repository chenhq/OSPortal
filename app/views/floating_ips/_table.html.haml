-# - form_tag ... do
%table.table.table-bordered.table-condensed#tab_servers
	%thead
		%tr
			%th
			%th IP地址
			%th IP类型
			%th 线路		
			%th 带宽
			%th 状态
			%th 绑定资源		
			%th 剩余有效期

	%tbody
		- @firewalls.each do |fw|
			%tr
				%td= check_box_tag "ipids", fw.id, false, firewellname: "#{fw.name}"
				%td= fw.name
				%td= fw.description
				%td 
				%td

